# Asks the user to input hours and their rate of pay. Calculates overtime due if any. 
# If the user does not input two numbers they are notified and it loops.

while True:
    try:
        x=input("How many hours did you work? " )
        if x == "done": break
        y=input("What is your rate of pay? " )
        if y == "done": break
        x=float(x)
        y=float(y)
        if x<40:
            p=x*y
        elif x>40:
            p=(x-40)*(1.5*y)+(y*40)
        print("Pay:", p)
        break
    except:
        print("Please enter two numbers.")
