#If a user inputs a number they receive a try again message. A user must input at least one letter as their name.

while True:
    x=input("What is your first name? ")
    if x.isalpha():
        break
    print("Try again.")
print("Hello",x)
